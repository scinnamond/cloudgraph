package org.cloudgraph.common.service;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;

/**
 * Common dispatcher functionality. 
 */
public abstract class DispatcherSupport {
    
	private static Log log = LogFactory.getFactory().getInstance(DispatcherSupport.class);
	
}
